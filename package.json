{
  "name": "takeuforward-frontend",
  "version": "1.0.0",
  "private": true,
  "description": "Interactive Wall Calendar — Takeuforward Frontend",
  "scripts": {
    "build": "cd calendar-app && npm install && npm run build",
    "start": "npx -y serve calendar-app/dist -s -l ${PORT:-3000}"
  },
  "dependencies": {
    "serve": "^14.2.0"
  },
  "engines": {
    "node": ">=18"
  }
}
